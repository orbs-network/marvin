{
  "name": "orchestrator",
  "version": "1.0.0",
  "description": "Orchestrates the endurance test by creating containers which produce load against the system",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "child-process-promise": "^2.2.1",
    "express": "^4.17.1",
    "knex": "^0.20.0",
    "mysql2": "^1.7.0",
    "ssh2": "^0.8.5"
  }
}
